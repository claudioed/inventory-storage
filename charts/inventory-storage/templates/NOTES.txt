{{ .Chart.Name }} has been deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Service:   {{ include "inventory-storage.fullname" . }} ({{ .Values.service.type }}, port {{ .Values.service.port }})

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
Ingress:   http://{{ .host }}{{ (index .paths 0).path }}
{{- end }}
{{- else }}
To reach it locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "inventory-storage.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/healthz
{{- end }}

{{- if not (or .Values.database.url .Values.database.existingSecret) }}

NOTE: no database.url / database.existingSecret was set — running with
IN-MEMORY adapters (state is lost on pod restart).
{{- end }}

{{- if not .Values.kafka.enabled }}
NOTE: kafka.enabled is false — this service publishes StockReserved /
ReservationRevoked to its own log only and does not join the cross-service
Kafka bus (wes-work-planning's usable-inventory projection will not update).
{{- end }}
