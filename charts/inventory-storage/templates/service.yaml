apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-storage.fullname" . }}
  labels:
    {{- include "inventory-storage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory-storage.selectorLabels" . | nindent 4 }}
